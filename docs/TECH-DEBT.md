# Tech Debt Tracker

> Auto-generated by [todo-tracker.yml](../.github/workflows/todo-tracker.yml) — updated weekly and on source changes.

**Generated:** 2026-03-18 02:43 UTC

## Summary

| Tag | Count |
|-----|-------|
| `TODO` | 216 |
| `FIXME` | 0 |
| `HACK` | 0 |
| `XXX` | 0 |
| `WORKAROUND` | 0 |
| **Total** | **216** |

## By Module

| Module | TODO | FIXME | HACK | Total |
|--------|------|-------|------|-------|
| HIT_ThesisWork | 3 | 0 | 0 | 3 |
| HeatmapVisualization | 4 | 0 | 0 | 4 |
| MobiusCore | 82 | 0 | 0 | 82 |
| MobiusWidgets | 38 | 0 | 0 | 38 |
| ProjectMobius | 73 | 0 | 0 | 73 |
| Visualization | 16 | 0 | 0 | 16 |

## All Items

### TODO

**Visualization/Private/HeatmapVisualizer.cpp**
- Line 232: `//TODO FIX THIS SIZE ISSUE`
- Line 332: `//TODO: covert to parallel for loop`
**Visualization/Private/DynamicPixelRenderingTexture.cpp**
- Line 71: `// Level of Service Colors for each band - in linear color format TODO: this needs to be a parameter`
- Line 105: `//TODO: add extra parameters for mip settings, compression settings, and srgb settings to give more control`
- Line 161: `if (!bIsBlurRequired && *(PixelPtrToUpdate + 2) > COLOR_TO_BYTE(BlurTriggerThreshold))// TODO: this value needs to either be a parameter or passed in - this is the lower band`
- Line 543: `* TODO: This needs to be fixed in the future, but for now we will use the Mat class. Potential fixes include OpenCV stream handling`
- Line 586: `std::vector<cv::Point2f> points;// TODO: need to test this with a vector of points`
- Line 751: `// TODO: future work may want to add a bool to save texture as grayscale`
- Line 912: `//TODO: ADD VARIABLES TO STORE ALREADY CALCULATED CVD COLOURS -- MAYBE BUT BLUR Could mess this up`
- Line 982: `//TODO: ADD VARIABLES TO STORE ALREADY CALCULATED CVD COLOURS -- MAYBE BUT BLUR Could mess this up`
**Visualization/Private/QuadTreeSubsystem.cpp**
- Line 102: `// check tree not null -TODO: Add bool if we using a quad or oct tree(eventually all be oct)`
**Visualization/Public/QuadTreeSubsystem.h**
- Line 71: `void RemoveEntityFromTree(FVector EntityLocation);//TODO: this needs to be a hash`
- Line 86: `//TODO: work out the columns and rows for the quadtree for spline graph to use`
**Visualization/Public/HeatmapVisualizer.h**
- Line 63: `// TODO: This method is likely going to need to be async so that we can update the render target in real time and not block the main thread`
**Visualization/Public/QuadTreeDataMap.h**
- Line 90: `//TODO: If a hash can be made to work out the tree location this would be a lot faster`
- Line 105: `* TODO: Add ID hashing to remove entities - this way we don't have to recursively search for the entity and can remove it in O(TreeDepth) time`
**MobiusWidgets/Private/Util/WidgetUtilHelpers.cpp**
- Line 195: `//TODO: FIX THIS METHOD`
- Line 207: `// TODO: this calculation is wrong`
**MobiusWidgets/Private/Slate/Components/SWindowTitleBarWidget.cpp**
- Line 59: `// TODO: Investigate further why calling the parent implementation here seems to cause issues.`
**MobiusWidgets/Private/Slate/Components/SPedestrianAgentHoverMeshWidget.cpp**
- Line 25: `//TODO: need to nullptr check for ParentWidget->this shouldn't happen as it is a child widget of the parent but improper removal may lead to this`
- Line 154: `// TODO: we should add small white placeholder text like ---`
**MobiusWidgets/Private/Slate/Components/SAgentFollowIndicator.cpp**
- Line 31: `//TODO: need to nullptr check for ParentWidget->this shouldn't happen as it is a child widget of the parent but improper removal may lead to this`
- Line 64: `//TODO: need to do this once, likely store a variable and check for nullptr`
- Line 109: `// TODO: we can improve this to be the only SMeshWidget, we just need to figure out how to handle the different textures for each instance`
**MobiusWidgets/Private/Slate/Components/SFieldAndTitleText.cpp**
- Line 220: `// TODO: add in padding if needed`
**MobiusWidgets/Private/UI/MobiusSettingPanel.cpp**
- Line 38: `// TODO:`
**MobiusWidgets/Private/UI/Components/PedestrianDataDisplay.cpp**
- Line 99: `// TODO: for now widget field 6 will be collapsed and not used as the gait speed is not yet implemented`
- Line 153: `WidgetHeadGridPanel->SetVisibility(ESlateVisibility::Visible);// should change to visible but self not hit testable -> TODO: update BP logic to handle this`
- Line 204: `// Check if the the required width and height are not the same as the current coefficients -> TODO: add a tolerance check here so we don't resize if the values are very close`
- Line 288: `//TODO: this is not right?? maybe being called before the widget is fully constructed? or a prepass has been completed`
- Line 317: `//TODO: we don't want to increase font size if the increase is less that 1px as this will cause the text to flicker/pop`
**MobiusWidgets/Private/UI/Components/BaseChangePedestrianMaterial.cpp**
- Line 109: `// TODO: this check need to be better and handled differently`
- Line 190: `// Does the MaterialInst match the starting material of the pedestrian? // TODO`
- Line 327: `// TODO: we may want to display a value to a user`
- Line 378: `//PropertySliderText->SetText(FText::FromString("Current " + CurrentMaterialProperty + ": " + FString::SanitizeFloat(CurrentVal))); //TODO this needs to be done in slider changed too`
- Line 408: `// TODO: until i figure the best way to handle resizing and scaling of the widget, we will hide the slider and text this way the widget will not be resized`
**MobiusWidgets/Private/UI/Components/FloorStatsWidget.cpp**
- Line 553: `// TODO: currently we have a limitation for updates that are sent to the overlay,`
**MobiusWidgets/Private/UI/Components/FlowSectionCounter.cpp**
- Line 63: `// TODO: backgrounds adjust padding/colors here`
- Line 107: `// TODO: determine if center/align inside our root grid cell here if needed`
**MobiusWidgets/Private/UI/Components/BaseLoadingWidget.cpp**
- Line 39: `// TODO: Future will customize this image to do more effects like change colour`
**MobiusWidgets/Private/UI/Components/Scalability/ScalabilitySettingWidget.cpp**
- Line 173: `// TODO: Add text style configuration for scalability buttons`
**MobiusWidgets/Private/UI/Components/Scalability/AdjustAvatarModel.cpp**
- Line 43: `//TODO: we have method duplicates that we need to rename and call where appropriate`
**MobiusWidgets/Private/UI/InWorld/FlowCounterWidget.cpp**
- Line 154: `// TODO: may need to assess if the ptr array count matches the uniform grid children count`
- Line 204: `// TODO: implement scroll logic`
- Line 256: `// TODO: get the padding correctly etc`
**MobiusWidgets/Private/UI/LoadingNotifyWidget.cpp**
- Line 72: `//TODO: Fix this - this binding doesn't work as expected but can be bound in the blueprint`
**MobiusWidgets/Public/UI/MoveableErrorWidget.h**
- Line 64: `//TODO: Change UTextBlocks to RichTextBlocks for better formatting and styling`
**MobiusWidgets/Public/UI/Components/BaseChangePedestrianMaterial.h**
- Line 75: `// TODO: more user settings and customization for the material types will be desired but for now they will be given fixed inputs`
- Line 150: `//TODO: currently doing non gender specific children`
- Line 183: `// TODO add the components for the material types that we want to change `
- Line 224: `// TODO add the properties for the material types that we want to change and ptrs to the material types`
**MobiusWidgets/Public/UI/Components/Scalability/AdjustAvatarModel.h**
- Line 33: `/** TODO: Listed likely functions we will need to implement once the core logic and avatar models are ready`
**MobiusWidgets/Public/UI/InWorld/FlowCounterWidget.h**
- Line 67: `// TODO: add methods for updating specific section counter values`
**MobiusWidgets/Public/UI/InWorld/AgentInfoDisplay.h**
- Line 52: `// TODO: should remove storing data from the widget and instead use a subsystem to manage this data`
**HeatmapVisualization/Private/HeatmapGenerator/HeatmapTextureGenerator.cpp**
- Line 187: `//TODO FIX THIS SIZE ISSUE`
**HeatmapVisualization/Public/QuadTree.h**
- Line 87: `//TODO: If a hash can be made to work out the tree location this would be a lot faster`
- Line 100: `* TODO: Add ID hashing to remove entities - this way we don't have to recursively search for the entity and can remove it in O(TreeDepth) time`
**HeatmapVisualization/Public/HeatmapGenerator/HeatmapTextureGenerator.h**
- Line 60: `// TODO: This method is likely going to need to be async so that we can update the render target in real time and not block the main thread`
**ProjectMobius/Private/Controller/MobiusController.cpp**
- Line 505: `// TODO: Current implementation is a placeholder and will load the file each time and then pick a random transform`
**ProjectMobius/Private/MassAI/MassObserverProcessor/Representation/AgentRepresentation_MOP.cpp**
- Line 150: `//TODO: need to do index size and offset check here`
- Line 155: `// TODO: this check will always fail on data swap as the frags are reset so sizes will be 0 like the offset`
- Line 311: `case EAgeDemographic::Ead_Child: // TODO: no female children yet`
**ProjectMobius/Private/MassAI/MassObserverProcessor/PedestrianInitializeMOP.cpp**
- Line 159: `//TODO: We have two methods that rely on the agent subsystem we should assign it to a variable and use it`
- Line 175: `//TODO: This is a poor assumption and should really be searching and checking`
- Line 267: `//TODO: MOVE INTO OWN OBSERVOR`
**ProjectMobius/Private/MassAI/SignalProcessors/EnableCollisionSignalProcessor.cpp**
- Line 34: `//TODO: will want to break this into two different signal processors, one for enabling collisions and one for disabling collisions -> this way the disable logic can be more complex and handle the disa`
**ProjectMobius/Private/MassAI/SubSystems/MassEntitySpawnSubsystem.cpp**
- Line 95: `//TODO: add a bool to check if the archetype has been created and if so, don't create it again`
- Line 144: `//TODO: We dont want to simulate time till this is done, also we need a better way to build shared fragment and update the archetype on data changes`
**ProjectMobius/Private/MassAI/SubSystems/AgentDataSubsystem.cpp**
- Line 119: `//TODO: optimize this so that the file path is not hardcoded and we only perform this operation once`
- Line 171: `//TODO: this is a temp fix but we need to find a better way to update loading text from this subsystems`
- Line 442: `else // no valid age demographic found -> TODO: for now just set it to adult but need to think on how we want to handle this`
- Line 556: `//TODO: should really be doing equal not compare `
- Line 819: `if (Hdf5Data.Meta != FHdf5SimulationMetadata())//TODO: need to improve this equality check logic`
- Line 874: `// TODO: refactor this to handle HDF5 data as well`
- Line 1019: `Position.X = PositionValue->ToSharedRef()->GetNumberField(StringCast<TCHAR>("x")); //TODO need to work out for different modeling studios`
- Line 1059: `Rotation = FRotator(0.0f, (-RotationValue -  90), 0.0f);//TODO: this is correct(for test data) and add method for different modeling studios`
- Line 1064: `Rotation = FRotator(0.0f, FMath::RadiansToDegrees(-RotationValue) - 90, 0.0f);//TODO: this is correct(for test data) and add method for different modeling studios`
- Line 1074: `Rotation = FRotator(0.0f, (-RotationValue -  90), 0.0f);//TODO: this is correct(for test data) and add method for different modeling studios`
- Line 1296: `if (Hdf5Format == EHdf5FormatType::Mobius)// TODO: need to add a axis parameter instead of relying on format type`
- Line 1520: `* @note TODO: Some agents appear to be moving but have 0 speed calculated. Investigate where`
- Line 1617: `// TODO: CHECK IF ANY HDF5 SPECIFIC FINALIZATION IS NEEDED`
- Line 1629: `Subsys->ProgressQueue.Enqueue(0.0f);// TODO: NEED to broadcast new load text here`
- Line 1669: `UAgentDataSubsystem* Subsys = OwnerSubsystem.Get(); //TODO: this may need to be a weak ptr check/and/or variable`
- Line 1679: `// TODO: this has no way to know progress of loading file at the moment so we need to think on how to do this better`
**ProjectMobius/Private/MassAI/SubSystems/MassRepresentation/MRS_RepresentationSubsystem.cpp**
- Line 248: `// TODO: no seperate gender logic yet`
- Line 434: `// TODO: refactor this method`
- Line 455: `//TODO: at the moment this method will be fine`
**ProjectMobius/Private/MassAI/MassProcessor/Collisions/PedestrianCollisionProcessor.cpp**
- Line 47: `//TODO: add tag for when we do toggle logic if we want to toggle the collision processor on and off`
- Line 74: `//TODO: We have two methods that rely on the agent subsystem we should assign it to a variable and use it`
- Line 101: `//TODO: below the query needs to be optimized so its scalable - current implementation is not scalable and is a hack together prototype`
- Line 120: `//TODO: We have two methods that rely on the agent subsystem we should assign it to a variable and use it`
- Line 252: `if (EntityCollision.Capsule.Get() == HoveredCapsuleComponent)//TODO: DOUBLE CLICK `
**ProjectMobius/Private/MassAI/MassProcessor/Analytics/FlowCounterProcessor.cpp**
- Line 63: `//TODO: check if paused -> don't want to keep executing if not needed`
**ProjectMobius/Private/MassAI/MassProcessor/Representation/AgentHeatmapProcessor.cpp**
- Line 114: `// TODO: Getting mallocs and realloc collisions with parallel -> maybe need to look at null check or something`
- Line 231: `// TODO: this loop is not parallelized, implement ParallelFor for performance and a threadsafe container for HeatmapLocations for simplicity`
**ProjectMobius/Private/MassAI/MassProcessor/Representation/DisplayAgentUIStatsProcessor.cpp**
- Line 190: `//TODO: Moving away from the agent mesh viewer, we will need to update this to use the new system ->once we have collisions`
**ProjectMobius/Private/MassAI/MassProcessor/Representation/NiagaraAgentRepProcessor.cpp**
- Line 93: `//TODO: Make flag for this so no constant gets etc`
- Line 98: `// Get the agent representation actor //TODO: this works but it could be better`
- Line 121: `//TODO: need to look at mass ai signals and how to use them -> this should be the equivalent to delegates and events -> and the reloading the shared fragments should only occur then`
- Line 129: `//TODO: this check does not work at all, it never returns true despite data changes!!`
- Line 373: `//TODO: FIX THIS - when going low spec to high the animations remain paused and only resume when we pause and unpause `
**ProjectMobius/Private/MassAI/MassProcessor/PedestrianMovementProcessor.cpp**
- Line 76: `// TODO: This processor needs breaking up into smaller processors as it is doing too much at once`
- Line 100: `//TODO: Move data to subsystem and get it from there so not constantly getting large data sets`
- Line 243: `//TODO: ****** when destroying respawning back this should only be called when needed not every execute ******`
- Line 324: `// TODO: This one should be at the start to only check once per call not per loop iteration per call`
**ProjectMobius/Private/Widgets/Simulation/SimulationSetupWidget.cpp**
- Line 94: `//TODO: Setup the editable text box to only accept numerical values`
**ProjectMobius/Private/Widgets/Simulation/SimulationPlayBar.cpp**
- Line 281: `//TODO: THIS NEEDS TO BE FIXED - currently only works when sim is paused first then dragged and unpaused not live adjust(somewhere is updating to previous value after new value)`
- ~~Line 299: resolved~~
- Line 308: `// Round current step to the nearest step size - TODO: this `
- Line 431: `// TODO: look into UWidgetUtilHelpers for this functionality and see if its better or useable`
**ProjectMobius/Private/Widgets/Simulation/TimeDilationWidget.cpp**
- Line 59: `//TODO: change to FTEXT so we can format the text better and have it localized`
**ProjectMobius/Public/MassAI/SubSystems/MassEntitySpawnSubsystem.h**
- Line 134: `void BuildPedestrianRepresentationFragmentData(); // TODO: add inputs to allow for customisation of the pedestrian representation currently hardcoded values`
**ProjectMobius/Public/MassAI/SubSystems/MassRepresentation/MRS_RepresentationSubsystem.h**
- Line 63: `static const FVatMovementFrames AvatarGaitSpeedBands[] = {             // TODO: need more, narrower bands: fastShuffle, VerySlowWalk etc.`
- Line 124: `* @param[float] AgentMaxSpeed The agents max speed TODO: this is a quick fix to create variations in agent movement speeds`
- Line 168: `//TODO: while still using bool on avatar choice in frag use this method to convert current setting to a bool`
**ProjectMobius/Public/MassAI/SubSystems/AgentDataSubsystem.h**
- Line 335: `* @warning TODO: Some agents appear to be moving but have 0 speed calculated.`
**ProjectMobius/Public/MassAI/MassProcessor/Representation/AgentHeatmapProcessor.h**
- Line 79: `/** Subsystem for heatmaps -- TODO: move this to its own processor */`
- Line 116: `UE::TConsumeAllMpmcQueue<FVector> LocationQueue; //TODO: Need to handle garbage collection of this queue`
**ProjectMobius/Public/MassAI/MassProcessor/PedestrianMovementProcessor.h**
- Line 78: `* TODO: This will be eventually replaced to be a lookup method but for now simple check`
**ProjectMobius/Public/MassAI/Fragments/EntityInfoFragment.h**
- Line 155: `uint8 showPedestrianStats = 0;// 0 = don't show, 1 = show, TODO: 2 = show and highlight`
- Line 163: `struct PROJECTMOBIUS_API FEntityCollisionFragment: public FMassFragment //TODO: this may want to be a FObjectWrapperFragment ?? TBD`
**ProjectMobius/Public/MassAI/Fragments/SharedFragments/SimulationFragment.h**
- Line 81: `// TODO: Add buffer method and not store all data in this struct`
- Line 82: `// TODO: This Map logic needs improving as it is not efficient with large data sets and looping over all data is poor`
**ProjectMobius/Public/MassAI/Fragments/SharedFragments/RepresenatationFragments/AgentNiagaraDataFrag.h**
- Line 53: `TArray<int32> MaleAdultAnimationStates; // TODO: likely when we bring dif age brackets we want all of them to be in one array`
- Line 77: `TArray<int32> FemaleAdultAnimationStates; // TODO: likely when we bring dif age brackets we want all of them to be in one array`
**ProjectMobius/Public/MassAI/Fragments/SharedFragments/RepresenatationFragments/AgentRepresenatationFragment.h**
- Line 90: `int32 NumberOfChildren = 0;// TODO: Maybe do different genders`
- Line 92: `//TODO: currently use bool to switch between low spec static effect and med VAT effect -> when we use enum we will use it here too`
- Line 135: `int32 NumberOfChildren = 0;// TODO: Maybe do different genders`
- Line 137: `//TODO: currently use bool to switch between low spec static effect and med VAT effect -> when we use enum we will use it here too`
**ProjectMobius/Public/Widgets/Simulation/SimulationPlayBar.h**
- Line 110: `* Decrement the current playback by 0.1 seconds - TODO: see whether its better to do by a timestep or by a fixed value`
**ProjectMobius/Public/Widgets/Simulation/TimeDilationWidget.h**
- Line 76: `//TODO: Change UTextblocks to RichTextBlocks for better formatting and styling`
**MobiusCore/Private/Interfaces/AssimpInterface.cpp**
- Line 37: `// May need to use rotate instead of unrotate -- TODO: See the difference between the two`
**MobiusCore/Private/BuildingGenerator/RuntimeMeshBuilder.cpp**
- Line 439: `FDatasmithRuntimeImportOptions ImportOptions;// TODO: set the import options`
- Line 490: `FDatasmithRuntimeImportOptions ImportOptions;// TODO: set the import options`
- Line 598: `// TODO:check valid data`
- Line 603: `/** TODO: if we get here and the runnable is not valid or the async loader is not valid we need to handle this`
**MobiusCore/Private/AsyncAssimpMeshLoader.cpp**
- Line 217: `//TODO: Need to workout a way to handle normals for WKT data -> filters just don't work for this`
- Line 375: `// TODO: add error handling to this Code and document`
- Line 780: `//TODO: this method needs to be refactored to use matrix transformations as it doesn't work when it comes to normals and this is where the issue is for translucent materials`
**MobiusCore/Private/Actors/FlowCounter.cpp**
- Line 50: `// TODO: Move these helpers to a utility class - likely will be useful elsewhere`
- Line 277: `// TODO: Need to clean up the line offset so we are consistent about ground vs line height as this is confusing`
- Line 357: `// TODO: setup alternative for VR`
- Line 526: `//TODO: likely need to communicate pillars location changing wait for changes to mass ai so we don't get data corruption/mismatches`
- Line 543: `// TODO: depending on mass ai logic we may want to reset the agents passed through counter as well`
- Line 638: `//TODO: Our z limits shouldn't be based on the trigger box extents as if the pillars are at different z locations, the trigger box will be rotated`
- Line 738: `// TODO: this is a temporary fix to account for the Z search limits being in world space and the line intersection check being in local space`
- Line 804: `else if (FlowCounter->PreviousTrackedAgentLocations.Contains(Data.AgentID)) // TODO: check if we actually want to check if intersected or disregard `
- Line 823: `// TODO: this is a temporary fix to account for the Z search limits being in world space and the line intersection check being in local space -> also minz may not be correctly set`
- Line 958: `// TODO: until we handle new sim time for large amounts of actors then we should only `
- Line 1079: `// TODO: Review this logic with Pete to ensure this is how we should be checking`
- Line 1148: `//TODO: If we store in multiple buckets in the case when start and end locations are the same we may want to remove from all buckets`
**MobiusCore/Private/Actors/HeatmapPixelTextureVisualizer.cpp**
- Line 238: `GenerateMeshVerticesUVsAndTriangles(MeshSize, FIntPoint(TextureWidth, TextureHeight), bIs3DHeatmap); // TODO work out a resolution for the mesh to be generated`
- Line 407: `//TODO: for now hardcode the radius as 11 - this is to be changed to a variable and be a float over integer for more precise locations`
- Line 447: `//TODO:OPTIMIZE`
- Line 472: `DynamicTexture->ClearTexture(); // TODO: create a buffer that represents clear so no need to keep doing loops`
- Line 502: `// TODO: this convert not needed as was part of user study test -> it should be converted into a material logic not cpu logic`
- Line 539: `//TODO:OPTIMIZE`
- Line 558: `DynamicTexture->ClearTexture(); // TODO: create a buffer that represents clear so no need to keep doing loops`
- Line 585: `// TODO: this convert not needed as was part of user study test -> it should be converted into a material logic not cpu logic`
- Line 852: `NumTriangles.X = FMath::CeilToInt32(MeshSize.X / 25); // TODO: working in meters so 100 is 1 meter so divide by 25 to get 4 triangles per meter`
- Line 854: `//TODO: this is a fix triangle size based on being to a 10 tenth of the mesh  size - this is as resolution goes up so does the number of triangles required`
- Line 1100: `float StepSize = 650.0f; // TODO: This is a hardcoded value for now for demo 500 ensures the building is covered - its too large for the mesh and has overflow around the edges `
**MobiusCore/Private/Components/FlowCounterSpawnerComponent.cpp**
- Line 216: `// TODO: Manipulate the rotation, size etc to fit door`
**MobiusCore/Private/Subsystems/StatisticSubsystem.cpp**
- Line 96: `// TODO: not really efficient to clear and re-add the flow counters every time this is called but for now it is fine`
- Line 102: `//TODO: we are going to need to expand this functionality when we increase the number of flow counters and their Z bounds`
- Line 151: `//TODO: this should be private method that we call after filtered in to correspond flowcounter groups - but prototype only using one so direct call to this is fine`
- Line 253: `// TODO: Instantaneous flow rate is the important one need to add`
**MobiusCore/Private/Subsystems/HeatmapSubsystem.cpp**
- Line 123: `//TODO: we just want to update the heatmap if the location is the same and values are different`
- Line 138: `FString HeatmapName = FString::Printf(TEXT("Heatmap_%i"), HeatmapIndex);// TODO: create a floor classification method based on array size`
- Line 176: `// TODO: this widget subsystem reference was removed to avoid a cross-module dependency on MobiusWidgets`
- Line 313: `// TODO: this sort of works for now but likely will cause problems later on`
- Line 586: `// TODO: THis method causing a small performance hit, need to investigate, likely due to the way task is executed and requiring game thread for some operations`
**MobiusCore/Private/Subsystems/TimeDilationSubSystem.cpp**
- Line 53: `UpdateMaxTimeSteps(FMath::CeilToInt(TotalTime / TimeBetweenSteps)); //TODO: TimeBetweenSteps should be read from file or a setting for the user`
- Line 166: `UpdateMaxTimeSteps(FMath::CeilToInt32(TotalTime / TimeBetweenSteps)); //TODO: TimeBetweenSteps should be read from file or a setting for the user`
- Line 269: `// TODO make this better`
**MobiusCore/Private/Subsystems/PerformanceUtilSubsystem.cpp**
- Line 78: `//TODO: investigate the best way to clear the delegate`
- Line 152: `//TODO: This method is simplistic and only calculates FPS based on DeltaTime.`
- Line 229: `// TODO: Extract private update method so we can update settings automatically or by users`
- Line 255: `// TODO: check if we can optimize this and reduce`
- Line 272: `// TODO: Extract private update method so we can update settings automatically or by users`
- Line 356: `// Using RHI we can get the available resolutions - TODO: we may want to look at refresh rate as well`
- Line 387: `// TODO: Extract private update method so we can update settings automatically or by users`
- Line 438: `// TODO: Extract private update method so we can update settings automatically or by users`
- Line 459: `//TODO: Below can be optimized with a lambda`
- Line 542: `CheckHardwareUsageAndApplyOptimizations();// TODO: current method is a stub, we need to implement it`
- Line 561: `//TODO: Our check for FPS is simplistic and only checks if the FPS is below a threshold. - we may want a range tolerance as well`
**MobiusCore/Private/GameInstances/ProjectMobiusGameInstance.cpp**
- Line 36: `// TODO: Set default values for these variables -- talk to peter if particular values are needed that`
**MobiusCore/Public/BuildingGenerator/RuntimeMeshBuilder.h**
- Line 194: `/** Internal method to set the material on the mesh - TODO: this will need to be converted to be called via a delegate  */`
- Line 307: `/** TODO: We eventually want to get the mesh material and apply our materials to it as a mask or material function to it`
**MobiusCore/Public/Actors/HeatmapPixelTextureVisualizer.h**
- Line 98: `// TODO: This method is likely going to need to be async so that we can update the render target in real time and not block the main thread`
- Line 136: `/**TODO: look over this as it is late when you wrote it`
- Line 285: `* TODO: Currently using integer values, this should be changed to a float value for more precise locations and scale to texture and mesh size`
- Line 288: `int32 CircleRadius = 110; // 110 = 1.1m for our scaled data - TODO: SORT THIS OUT FOR BETTER SCALING`
- Line 320: `* Method to calculate the number of triangles needed for the mesh when using 3D heatmaps - TODO: this may be needed for all heatmap types due to the cost asscoiated with the mesh generation`
- Line 372: `int32 ScaledCircleSize; // TODO: This should be a float value for more precise locations and scale to texture and mesh size`
**MobiusCore/Public/Actors/FlowCounter.h**
- Line 20: `// Structs used internally for bucketing agents -> TODO: Move it to the FlowCounterStructs.h file as we may want to use it elsewhere`
- Line 262: `// TODO: we will need to also bucket this off as a per segment rolling average as we want that data`
- Line 325: `/* TODO: this property is redundant, we should just update methods to use the line that would be at the base of the pillars`
- Line 332: `/* TODO: we will want a reference to a user widget this way we can create a cpp version in the widget module and`
**MobiusCore/Public/EnumsAndStructs/AgentMeshViewer.h**
- Line 26: `float AgentSpeedFlux = 0.0f;// TODO: name of variable doesn't make sense`
- Line 28: `//TODO: // Add more properties as needed, such as direction, health, etc.`
**MobiusCore/Public/Subsystems/MobiusControllerSubsystem.h**
- Line 87: `/**TODO: TBD if we can or want to use interfaces with mass ai entities, if so then we can use this method to perform a line trace`
**MobiusCore/Public/Subsystems/StatisticSubsystem.h**
- Line 57: `/** TODO: this will be used to pull logic from the heatmap subsystem and the floor stats widget so only one subsystem is used`
- ~~Line 148: resolved — flow equations verified against pedestrian flow literature~~
**MobiusCore/Public/Subsystems/TimeDilationSubSystem.h**
- Line 41: `/** Delegate to broadcast the new simulation time when it changes */ //TODO: do we need this delegate? left comment for now`
- Line 178: `//TODO: @Peter -- This an assumption to be clarified but I am assuming all timesteps are calculated`
**MobiusCore/Public/Subsystems/PerformanceUtilSubsystem.h**
- Line 20: `DECLARE_MULTICAST_DELEGATE(FOnManualScalabilityChanged);//TODO: investigate whether separate param delegates will be better for performance or not`
**MobiusCore/Public/Subsystems/HeatmapSubsystem.h**
- Line 83: `// TODO: bound it to the OnMeshBuilt delegate -> this needs refactoring to do as this needs moving to prevent circular dependencies`
- Line 101: `* TODO: we will in future want to pass more parameters to this function like type of heatmap, size, etc`
- Line 152: `//TODO: this method needs to be placed in a more appropriate place as it is not really a heatmap method but we have bound the visualiser chart logic to this system`
**MobiusCore/Public/GameInstances/ProjectMobiusGameInstance.h**
- Line 100: `//TODO: convert methods below so we can broadcast events when the data is changed`
- Line 253: `//TODO: we will bring datatables back as it makes it to manage and requires less memory allocation`
- Line 318: `//TODO: we will bring datatables back as it makes it to manage and requires less memory allocation`
**HIT_ThesisWork/Private/ExperimentScorer.cpp**
- Line 209: `Result = FFileHelper::SaveStringToFile(FString("Test Data"), *CSVFilePath);//TODO: Replace "Test Data" with the actual data and look at my other code for how to format the data and add more complexity`
- Line 345: `bColourDeficiencyOverrided = false; // TODO: change to true once images for thesis done`
**HIT_ThesisWork/Public/ExperimentScorer.h**
- Line 285: `const int32 SimulationLimit = 5;// TODO: change this if recruitment increases`

