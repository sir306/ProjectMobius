{
  "name": "mobius-server",
  "private": true,
  "version": "1.0.0",
  "description": "A simple local WebSocket server",
  "main": "MobiusServer.js",
  "bin": {
    "mobius-server": "./MobiusServer.js"
  },
  "scripts": {
    "clean": "rimraf bundle dist || rmdir /s /q bundle dist 2>nul || true",
    "bundle:win": "pkg . --targets node20-win-x64 --output bundle/mobius-server-win-x64.exe",
    "bundle:mac-x64": "pkg . --targets node20-macos-x64 --output bundle/mobius-server-macos-x64",
    "bundle:mac-arm64": "pkg . --targets node20-macos-arm64 --output bundle/mobius-server-macos-arm64",
    "bundle": "node -e \"const os=require('os'); const p=process.platform; if(p==='win32') process.exit(0); else if(p==='darwin'){console.log('mac'); process.exit(0);} else {console.error('Unsupported platform'); process.exit(1);}\" && echo \"Use bundle:win or bundle:mac-* directly\"",
    "start": "node MobiusServer.js",
    "postinstall": "node -e \"console.log('Dependencies installed')\""
  },
  "pkg": {
    "assets": [
      "config.json"
    ]
  },
  "dependencies": {
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "pkg": "^5.8.1",
    "rimraf": "^6.0.1"
  }
}
